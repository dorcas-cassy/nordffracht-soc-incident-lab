<group name="nordfracht,sshd,recon,">
  <!-- Nmap's SSH version probe can stop during key exchange. -->
  <rule id="100100" level="5">
    <if_sid>5722</if_sid>
    <program_name>sshd</program_name>
    <match>kex_exchange_identification: Connection closed by remote host</match>
    <description>NordFracht: SSH key exchange closed before authentication; investigate possible scanning</description>
    <group>authentication_failed,</group>
  </rule>
</group>
